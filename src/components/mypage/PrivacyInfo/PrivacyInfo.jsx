import { Box } from '@mui/material';
import styled from './privacy.info.module.css';

const PrivacyInfo = () => {
  return (
    <Box className={styled.privacy}>

    </Box>
  )
};

export default PrivacyInfo;
